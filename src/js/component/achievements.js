import React, { Component } from "react";
import propTypes from "prop-types";
export const Achievements = ({title,data})=> (

<div className="col text-justify"> {data} <br /> <p >{title}</p> </div>


	
);